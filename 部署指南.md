# 🚀 多人交易平台部署指南

## 问题说明

你的应用包含两部分：
1. **前端**：HTML/CSS/JavaScript（`index.html`, `admin.html`）
2. **后端**：Python Flask + SocketIO 服务器（`server_multiplayer.py`）

**Netlify 只能托管静态网站**，无法运行 Python 后端，所以会出现"未连接到服务器"的错误。

---

## ✅ 解决方案：使用 Render 部署（免费）

### 第一步：准备代码

我已经为你创建了必要的配置文件：
- ✅ `Procfile` - 告诉 Render 如何启动服务器
- ✅ `render.yaml` - Render 配置文件
- ✅ `runtime.txt` - Python 版本
- ✅ 修改了 `server_multiplayer.py` 支持动态端口

### 第二步：上传到 GitHub

1. 访问 https://github.com/new
2. 创建一个新仓库（例如：`traders0123`）
3. 在你的项目文件夹中打开命令行，运行：

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/traders0123.git
git push -u origin main
```

### 第三步：部署到 Render

1. 访问 https://render.com/ 并注册/登录
2. 点击 **"New +"** → **"Web Service"**
3. 连接你的 GitHub 仓库
4. 配置如下：
   - **Name**: `traders0123`（或任意名称）
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python server_multiplayer.py`
   - **Plan**: 选择 **Free**
5. 点击 **"Create Web Service"**

### 第四步：等待部署完成

- Render 会自动安装依赖并启动服务器
- 部署完成后，你会得到一个 URL，例如：`https://traders0123.onrender.com`

### 第五步：测试

访问以下地址：
- 用户界面：`https://traders0123.onrender.com/`
- 管理员界面：`https://traders0123.onrender.com/admin`

---

## 🎯 方案二：使用 Railway（备选，免费额度）

如果 Render 不行，可以试试 Railway：

1. 访问 https://railway.app/
2. 注册并连接 GitHub
3. 点击 **"New Project"** → **"Deploy from GitHub repo"**
4. 选择你的仓库
5. Railway 会自动检测 Python 项目并部署

---

## 🎯 方案三：使用 Fly.io（备选）

1. 访问 https://fly.io/ 并注册
2. 安装 Fly CLI：https://fly.io/docs/hands-on/install-flyctl/
3. 在项目文件夹运行：

```bash
fly launch
fly deploy
```

---

## ⚠️ 重要提示

### 免费套餐限制

- **Render 免费版**：
  - 15分钟无活动后会休眠
  - 首次访问需要等待30秒唤醒
  - 每月750小时免费运行时间
  
- **解决休眠问题**：可以使用 UptimeRobot 等服务每5分钟 ping 一次你的网站

### WebSocket 支持

- Render、Railway、Fly.io 都支持 WebSocket
- 确保部署后测试 Socket.IO 连接是否正常

---

## 📝 常见问题

### Q: 为什么 Netlify 不行？
A: Netlify 只能托管静态文件（HTML/CSS/JS），无法运行 Python 后端服务器。

### Q: 部署后显示"未连接到服务器"？
A: 检查浏览器控制台（F12）的错误信息，确认 WebSocket 连接地址是否正确。

### Q: 如何修改管理员密码？
A: 编辑 `server_multiplayer.py` 第 18 行：
```python
ADMIN_PASSWORD = 'admin123'  # 改成你想要的密码
```

### Q: 如何查看服务器日志？
A: 在 Render 控制台的 "Logs" 标签页可以查看实时日志。

---

## 🎉 部署成功后

1. 分享你的网站链接给用户
2. 使用 `/admin` 路径访问管理员控制台
3. 默认管理员密码：`admin123`

---

## 需要帮助？

如果遇到问题，请提供：
1. 部署平台名称（Render/Railway/Fly.io）
2. 错误信息截图
3. 浏览器控制台（F12）的错误日志
